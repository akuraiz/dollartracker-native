# Windows Setup

Use GitHub Desktop for repository history and VS Code for editing.

## Install and run

```powershell
npm install
npm run verify
npm start
```

The local app runs at `http://localhost:4173`.

## Safe rhythm

```text
Pull → edit → test → verify → commit → push
```
